# Install this as an hourly cron job on the Compose host. The path matches the
# bind mount used by docker-compose.yaml; adjust it only if the mount changes.
#
# Delete leaked Send upload files older than 7 days (and a bit).
0 * * * * find /var/lib/send/uploads/ -type f -mmin +10130 -delete
